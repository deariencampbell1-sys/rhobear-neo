---
manifest_paths:
  - .rhobear/surfaces/neo.worker.json
---

# RHOBEAR Neo surface companion

This import-candidate manifest records the observed worker boundary without
turning transport health into authority to fix or merge:

- the service/public response and DeepSeek Direct route were observed;
- webhook wake, credit entitlement, and tool-enabled fix-forward remain
  `UNVERIFIED` without an owner-authorized end-to-end run;
- automatic merge is deliberately `INTENTIONAL_OMIT` by default because
  `NEO_AUTO_MERGE=false` remains the safety baseline.

After architecture PR #4 and this source PR merge, the manifest becomes the
feature/availability truth for this imported candidate. It does **not** resolve
whether the divergent VPS live tree or this local import is canonical.

Stable-ID schema dependency:
https://github.com/deariencampbell1-sys/rhobear-product-architecture/pull/4

No provider, merge-policy, runtime, deployment, or webhook behavior changes in
this source-record PR.
